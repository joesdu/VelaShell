<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:VelaShell.ViewModels"
        xmlns:behaviors="using:VelaShell.Behaviors"
        xmlns:models="clr-namespace:VelaShell.Core.Models;assembly=VelaShell.Core"
        xmlns:loc="using:VelaShell.Localization"
        mc:Ignorable="d" d:DesignWidth="500"
        x:Class="VelaShell.Views.ConnectionProfileView"
        x:DataType="vm:ConnectionProfileViewModel"
        Title="{loc:Localize Profile_Title}"
        Width="508"
        SizeToContent="Height"
        CanResize="False"
        WindowDecorations="None"
        TransparencyLevelHint="Transparent"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">
  <Window.Styles>
    <Style Selector="AdornerLayer">
      <Setter Property="DefaultFocusAdorner" Value="{x:Null}" />
    </Style>
    <!-- 表单标签 -->
    <Style Selector="TextBlock.field-label">
      <Setter Property="Foreground" Value="{DynamicResource VelaTextSecondary}" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="FontWeight" Value="Medium" />
      <Setter Property="Margin" Value="0,0,0,5" />
    </Style>
    <!-- 字段下方的内联校验提示:按钮灰着的时候,得让人看见是为什么。
         没有错误时 IsVisible=false,整行不占高度,布局不跳。 -->
    <Style Selector="TextBlock.field-error">
      <Setter Property="Foreground" Value="{DynamicResource VelaError}" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize10}" />
      <Setter Property="Margin" Value="0,4,0,0" />
      <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style Selector="TextBox">
      <Setter Property="Background" Value="{DynamicResource VelaBgInput}" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextPrimary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="FontFamily" Value="{DynamicResource VelaUiMonoFont}" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style Selector="ComboBox">
      <Setter Property="Background" Value="{DynamicResource VelaBgInput}" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextPrimary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style Selector="NumericUpDown">
      <Setter Property="Background" Value="{DynamicResource VelaBgInput}" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextPrimary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="FontFamily" Value="{DynamicResource VelaUiMonoFont}" />
    </Style>
    <!-- 底部按钮:描边(测试/保存)与主色(连接) -->
    <Style Selector="Button.dlg-outline">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextSecondary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="FontWeight" Value="Medium" />
    </Style>
    <Style Selector="Button.dlg-primary">
      <Setter Property="Background" Value="{DynamicResource VelaAccent}" />
      <Setter Property="Foreground" Value="{DynamicResource VelaAccentForeground}" />
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="16,0" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="FontWeight" Value="Medium" />
    </Style>
    <!-- 错误信息旁的「复制」小按钮:比页脚按钮矮一圈,免得抢走测试/保存的分量 -->
    <Style Selector="Button.copy-chip">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextSecondary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Height" Value="24" />
      <Setter Property="Padding" Value="8,0" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize10}" />
      <Setter Property="VerticalAlignment" Value="Top" />
    </Style>
    <Style Selector="Button.copy-chip:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource VelaBgHover}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaBorderPrimary}" />
    </Style>
    <!-- 协议标签页 -->
    <Style Selector="Border.proto-tab">
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="VerticalAlignment" Value="Bottom" />
    </Style>
    <Style Selector="TextBlock.proto-label">
      <Setter Property="FontFamily" Value="{DynamicResource VelaUiMonoFont}" />
      <Setter Property="FontSize" Value="{DynamicResource VelaFontSize11}" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style Selector="Button.proto-tab">
      <Setter Property="FocusAdorner" Value="{x:Null}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0,0,0,2" />
      <Setter Property="Foreground" Value="{DynamicResource VelaTextTertiary}" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="Height" Value="32" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="Background" Duration="0:0:0.12" Easing="CubicEaseOut" />
          <BrushTransition Property="BorderBrush" Duration="0:0:0.12" Easing="CubicEaseOut" />
          <BrushTransition Property="Foreground" Duration="0:0:0.12" Easing="CubicEaseOut" />
        </Transitions>
      </Setter>
    </Style>
    <Style Selector="Button.proto-tab:pointerover">
      <Setter Property="Foreground" Value="{DynamicResource VelaTextSecondary}" />
      <Setter Property="Background" Value="{DynamicResource VelaBgHover}" />
    </Style>
    <!-- 选中态只变前景色;下划线由标签条底部的滑动指示条统一呈现(在 SSH/SFTP 间滑动)。 -->
    <Style Selector="Button.proto-tab.selected">
      <Setter Property="Foreground" Value="{DynamicResource VelaAccent}" />
    </Style>
    <!-- 焦点记号只给键盘导航(:focus-visible)。此前用 :focus,鼠标点完标签后强调色
         填充+描边会一直挂在按钮上直到焦点移走 —— 看起来像卡住的高亮,是这个窗口
         标签切换观感崩坏的主因。 -->
    <Style Selector="Button.proto-tab:focus-visible">
      <Setter Property="Background" Value="{DynamicResource VelaAccentDim}" />
    </Style>
    <!-- Fluent 的焦点层位于 Button 背景之上;只覆盖这个类,
         使其系统强调色无法替换 Vela 的焦点记号。 -->
    <Style Selector="Button.proto-tab:focus-visible /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource VelaAccentDim}" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaAccent}" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>
    <Style Selector="Button.proto-tab:focus-visible /template/ Border#PART_FocusAdorner">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{DynamicResource VelaAccent}" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>
  </Window.Styles>

  <!-- 自绘对话框卡片(设计:圆角 8、外描边、投影) -->
  <Border CornerRadius="8"
          Background="{DynamicResource VelaBgSurface}"
          BorderBrush="{DynamicResource VelaBorderSecondary}"
          BorderThickness="1"
          BoxShadow="{DynamicResource VelaShadowWindow}"
          Margin="16">
    <!-- 头部/页签/页脚定高,中间的表单是唯一会伸缩的一行:窗口 SizeToContent="Height"
         仍按内容收缩,但被代码后置钉在屏幕工作区内(见 ApplyScreenBounds),
         超出的部分由表单区自己滚动 —— 插件协议(S3 有十来个字段)不会再把
         底部的保存/连接按钮顶到屏幕外面去。 -->
    <!-- 头部/页签/反馈/页脚定高,中间的表单是唯一会伸缩的一行。 -->
    <Grid RowDefinitions="Auto,Auto,*,Auto,Auto">

      <!-- Header: plug 图标 + 标题 + 关闭 -->
      <Border Grid.Row="0" Height="48" Padding="20,0"
              PointerPressed="Header_PointerPressed"
              Background="Transparent"
              BorderThickness="0,0,0,1"
              BorderBrush="{DynamicResource VelaBorderPrimary}">
        <Grid ColumnDefinitions="Auto,8,*,Auto">
          <PathIcon Grid.Column="0" Width="16" Height="16"
                    Data="M16 7V3h-2v4h-4V3H8v4c-1.1 0-2 .9-2 2v5.5L9.5 18v3h5v-3L18 14.5V9c0-1.1-.9-2-2-2z"
                    Foreground="{DynamicResource VelaAccent}"
                    VerticalAlignment="Center" />
          <TextBlock Grid.Column="2" Text="{loc:Localize Profile_Title}"
                     Foreground="{DynamicResource VelaTextPrimary}"
                     FontSize="{DynamicResource VelaFontSize14}" FontWeight="SemiBold"
                     VerticalAlignment="Center" />
          <Button Grid.Column="3" Width="24" Height="24"
                  Background="Transparent" Padding="0" CornerRadius="3"
                  Cursor="Hand" Command="{Binding CancelCommand}">
            <PathIcon Width="14" Height="14"
                      Data="M19 6.41 17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
                      Foreground="{DynamicResource VelaTextTertiary}" />
          </Button>
        </Grid>
      </Border>

      <!-- 协议标签页:SSH/SFTP/FTP 内建 + 插件协议(动态);Telnet/串口保持禁用 -->
      <Border Grid.Row="1" Height="36" Padding="20,0"
              BorderThickness="0,0,0,1"
              BorderBrush="{DynamicResource VelaBorderPrimary}">
        <Panel x:Name="ProtoTabsPanel" VerticalAlignment="Bottom">
          <StackPanel Orientation="Horizontal">
            <Button x:Name="SshTab" Classes="proto-tab" Classes.selected="{Binding IsSshSelected}"
                    Command="{Binding SelectConnectionTypeCommand}"
                    CommandParameter="{x:Static models:ConnectionType.SSH}">
              <TextBlock Classes="proto-label" Text="SSH" FontWeight="Medium" />
            </Button>
            <Button x:Name="SftpTab" Classes="proto-tab" Classes.selected="{Binding IsSftpSelected}"
                    Command="{Binding SelectConnectionTypeCommand}"
                    CommandParameter="{x:Static models:ConnectionType.SFTP}">
              <TextBlock Classes="proto-label" Text="SFTP" FontWeight="Medium" />
            </Button>
            <Button x:Name="FtpTab" Classes="proto-tab" Classes.selected="{Binding IsFtpSelected}"
                    Command="{Binding SelectConnectionTypeCommand}"
                    CommandParameter="{x:Static models:ConnectionType.FTP}">
              <TextBlock Classes="proto-label" Text="FTP" FontWeight="Medium" />
            </Button>
            <!-- 插件协议页签:内容来自已安装插件的清单声明,**不装载任何插件程序集**;
                 用户点到某个页签才触发它的惰性激活并补齐下面的设置表单。
                 因此新增一种协议不需要动这个文件。 -->
            <ItemsControl ItemsSource="{Binding PluginProtocols}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:PluginProtocolTabViewModel">
                  <Button Classes="proto-tab plugin-proto"
                          Tag="{Binding Id}"
                          Classes.selected="{Binding IsSelected}"
                          Command="{Binding $parent[ItemsControl].((vm:ConnectionProfileViewModel)DataContext).SelectPluginProtocolCommand}"
                          CommandParameter="{Binding Id}">
                    <TextBlock Classes="proto-label" Text="{Binding DisplayName}" FontWeight="Medium" />
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!-- Telnet 与串口都已由插件提供(velashell-plugins 仓库的
                 VelaShell.Plugin.Telnet / VelaShell.Plugin.Serial),页签走上面那条动态列表 ——
                 宿主不再认识任何一种具体协议,这里也就不再有禁用的占位页签。 -->
          </StackPanel>
          <!-- 滑动下划线:2px 强调色,在各协议页签间平移;位置由代码后置按按钮实际边界驱动。 -->
          <Border x:Name="ProtoTabIndicator"
                  Height="2"
                  Background="{DynamicResource VelaAccent}"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Bottom"
                  IsVisible="False"
                  IsHitTestVisible="False">
            <Border.Transitions>
              <Transitions>
                <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.18" Easing="CubicEaseOut" />
                <DoubleTransition Property="Width" Duration="0:0:0.18" Easing="CubicEaseOut" />
              </Transitions>
            </Border.Transitions>
          </Border>
        </Panel>
      </Border>

      <!-- 表单 -->
      <!-- 滚动条走全应用的两态方案(Themes/ScrollBarThemes.axaml),不在这里按住 AllowAutoHide。
           这里原先写了 AllowAutoHide=False,想解决的是"表单被高度上限截断、用户以为字段没了";
           但那个开关的含义是【常驻展开】(ScrollBar.UpdateIsExpandedState:不许自动隐藏就是
           IsExpanded=true),于是新建连接一打开,右侧就顶着一条带滑道和两端箭头的粗滚动条,
           和全应用其它地方都不一样。本主题的未激活态本来就是一根可见的 2px 细条(不是 Fluent
           那种整条隐形),截断照样看得见;悬停又已经是即时展开,affordance 不缺。 -->
      <ScrollViewer Grid.Row="2"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
      <StackPanel Margin="20,18" Spacing="14">

        <!-- 主机名/IP + 端口。插件协议可以改写这一格的标签与占位(S3 的「服务端点」),
             也可以改写它的**形态** —— 串口把它变成一个可刷新、可手输的设备下拉。
             端口那一格在协议声明 NoEndpoint 时整格收起(串口是一根线,SQLite 是一个文件,
             端口填什么都不会被用上)。 -->
        <Grid ColumnDefinitions="*,12,Auto">
          <StackPanel Grid.Column="0">
            <TextBlock Classes="field-label" Text="{Binding HostLabel}" />
            <TextBox Text="{Binding Host}" PlaceholderText="{Binding HostPlaceholder}"
                     IsVisible="{Binding HostIsText}" />
            <!-- 只读下拉:值只能取自候选项。 -->
            <ComboBox ItemsSource="{Binding HostChoices}" SelectedItem="{Binding SelectedHostChoice}"
                      IsVisible="{Binding HostIsChoice}" HorizontalAlignment="Stretch">
              <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="vm:PluginChoiceItem">
                  <TextBlock Text="{Binding Label}" />
                </DataTemplate>
              </ComboBox.ItemTemplate>
            </ComboBox>
            <!-- 可编辑下拉 + 刷新:串口是这一档。列表是便利而非白名单 ——
                 没插的适配器、容器里映射进来的 /dev/ttyS10 都必须填得进去。
                 PluginChoiceItem.ToString() 返回的是**落盘值**,所以选中一项之后
                 文本框里是 COM3 而不是那条带描述的展示文案。 -->
            <Grid ColumnDefinitions="*,4,Auto" IsVisible="{Binding HostIsEditableChoice}">
              <ComboBox Grid.Column="0" IsEditable="True"
                        ItemsSource="{Binding HostChoices}"
                        Text="{Binding Host}"
                        PlaceholderText="{Binding HostPlaceholder}"
                        HorizontalAlignment="Stretch">
                <ComboBox.ItemTemplate>
                  <DataTemplate x:DataType="vm:PluginChoiceItem">
                    <TextBlock Text="{Binding Label}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
              <Button Grid.Column="2" Width="30" Height="30" Padding="0"
                      Background="Transparent" CornerRadius="3" Cursor="Hand"
                      IsVisible="{Binding HostIsDynamicChoice}"
                      Command="{Binding RefreshHostChoicesCommand}"
                      ToolTip.Tip="{loc:Localize Profile_RefreshDevices}"
                      VerticalAlignment="Bottom">
                <PathIcon Width="13" Height="13"
                          Data="M17.65 6.35A7.958 7.958 0 0 0 12 4a8 8 0 1 0 7.73 10h-2.08A6 6 0 1 1 12 6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"
                          Foreground="{DynamicResource VelaTextTertiary}" />
              </Button>
            </Grid>
            <TextBlock Classes="field-error" Text="{Binding HostError}"
                       IsVisible="{Binding HostError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
          </StackPanel>
          <StackPanel Grid.Column="2" Width="90" IsVisible="{Binding ShowPortField}">
            <TextBlock Classes="field-label" Text="{loc:Localize Port}" />
            <NumericUpDown Value="{Binding Port}" Minimum="1" Maximum="65535"
                           Increment="1" FormatString="0" ShowButtonSpinner="False" />
            <TextBlock Classes="field-error" Text="{Binding PortError}"
                       IsVisible="{Binding PortError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
          </StackPanel>
        </Grid>

        <!-- 用户名 + 认证方式(插件协议可改写用户名标签,如 Access Key ID;
             声明了 NoCredentials 的协议(Telnet)整行收起 —— 它的登录在带内进行) -->
        <Grid ColumnDefinitions="*,12,*" IsVisible="{Binding ShowCredentialFields}">
          <StackPanel Grid.Column="0">
            <TextBlock Classes="field-label" Text="{Binding UsernameLabel}" />
            <TextBox Text="{Binding Username}" PlaceholderText="root" />
            <TextBlock Classes="field-error" Text="{Binding UsernameError}"
                       IsVisible="{Binding UsernameError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
          </StackPanel>
          <StackPanel Grid.Column="2" IsVisible="{Binding RequiresSshAuth}">
            <TextBlock Classes="field-label" Text="{loc:Localize AuthMethodLabel}" />
            <ComboBox SelectedIndex="{Binding AuthMethodIndex}">
              <ComboBoxItem Content="{loc:Localize Profile_PasswordAuth}" />
              <ComboBoxItem Content="{loc:Localize Profile_KeyAuth}" />
            </ComboBox>
          </StackPanel>
          <!-- FTP 没有私钥认证,这一格换成加密方式 -->
          <StackPanel Grid.Column="2" IsVisible="{Binding IsFtpSelected}">
            <TextBlock Classes="field-label" Text="{loc:Localize Ftp_Encryption}" />
            <ComboBox SelectedIndex="{Binding FtpEncryptionIndex}">
              <ComboBoxItem Content="{loc:Localize Ftp_EncryptionAuto}" />
              <ComboBoxItem Content="{loc:Localize Ftp_EncryptionExplicit}" />
              <ComboBoxItem Content="{loc:Localize Ftp_EncryptionImplicit}" />
              <ComboBoxItem Content="{loc:Localize Ftp_EncryptionNone}" />
            </ComboBox>
          </StackPanel>
        </Grid>

        <!-- FTP 专属:匿名登录 + 被动模式 -->
        <StackPanel Orientation="Horizontal" Spacing="16" IsVisible="{Binding IsFtpSelected}">
          <CheckBox IsChecked="{Binding FtpAnonymous}"
                    Padding="0" MinHeight="0" VerticalAlignment="Center">
            <TextBlock Text="{loc:Localize Ftp_Anonymous}"
                       Margin="6,0,0,0"
                       Foreground="{DynamicResource VelaTextSecondary}"
                       FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium" />
          </CheckBox>
          <CheckBox IsChecked="{Binding FtpPassive}"
                    Padding="0" MinHeight="0" VerticalAlignment="Center"
                    ToolTip.Tip="{loc:Localize Ftp_PassiveHint}">
            <TextBlock Text="{loc:Localize Ftp_Passive}"
                       Margin="6,0,0,0"
                       Foreground="{DynamicResource VelaTextSecondary}"
                       FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium" />
          </CheckBox>
        </StackPanel>

        <!-- 明文 FTP 的安全提示:与 SSH 不同,它没有主机身份校验,也不加密 -->
        <Border Padding="10,7" CornerRadius="4"
                Background="{DynamicResource VelaBgInput}"
                BorderBrush="{DynamicResource VelaWarning}" BorderThickness="1"
                IsVisible="{Binding ShowFtpPlaintextWarning}">
          <TextBlock Text="{loc:Localize Ftp_PlaintextWarn}"
                     Foreground="{DynamicResource VelaTextSecondary}"
                     FontSize="{DynamicResource VelaFontSize11}" TextWrapping="Wrap" />
        </Border>

        <!-- 插件协议的设置表单。字段声明来自插件的 ProtocolDescriptor,宿主按 Kind 渲染 ——
             插件因此不需要写一行界面代码,而连接对话框的布局、主题与校验仍归宿主统一负责。 -->
        <TextBlock Text="…" IsVisible="{Binding IsPluginLoading}"
                   Foreground="{DynamicResource VelaTextMuted}"
                   FontSize="{DynamicResource VelaFontSize11}" />
        <ItemsControl ItemsSource="{Binding PluginFields}" IsVisible="{Binding IsPluginSelected}">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:PluginProtocolFieldViewModel">
              <!-- 标了 IsAdvanced 的字段(分片大小、并发数之类的调优项)默认收进「高级选项」:
                   S3 一口气声明了十来个字段,全铺出来窗口就长到屏幕外了。 -->
              <StackPanel Margin="0,0,0,12" Spacing="4" IsVisible="{Binding IsRowVisible}">
                <TextBlock Classes="field-label" Text="{Binding Label}" IsVisible="{Binding !IsToggle}" />
                <TextBox Text="{Binding Text}" PlaceholderText="{Binding Placeholder}" IsVisible="{Binding IsText}" />
                <TextBox Text="{Binding Text}" PasswordChar="•" IsVisible="{Binding IsPassword}" />
                <ComboBox ItemsSource="{Binding Choices}" SelectedItem="{Binding SelectedChoice}"
                          IsVisible="{Binding IsChoice}" HorizontalAlignment="Stretch">
                  <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:PluginChoiceItem">
                      <TextBlock Text="{Binding Label}" />
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
                <!-- 可编辑下拉:候选项是便利,不是白名单。波特率就是活例子 ——
                     表里给九个常用值,但 250000(Marlin)、76800(工业模块)都得填得进去。
                     动态形态(候选项向插件现取)再多一个刷新按钮。 -->
                <Grid ColumnDefinitions="*,4,Auto" IsVisible="{Binding IsEditableChoice}">
                  <ComboBox Grid.Column="0" IsEditable="True"
                            ItemsSource="{Binding Choices}"
                            Text="{Binding Text}"
                            PlaceholderText="{Binding Placeholder}"
                            HorizontalAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                      <DataTemplate x:DataType="vm:PluginChoiceItem">
                        <TextBlock Text="{Binding Label}" />
                      </DataTemplate>
                    </ComboBox.ItemTemplate>
                  </ComboBox>
                  <Button Grid.Column="2" Width="30" Height="30" Padding="0"
                          Background="Transparent" CornerRadius="3" Cursor="Hand"
                          IsVisible="{Binding IsDynamicChoice}"
                          Command="{Binding RefreshChoicesCommand}"
                          ToolTip.Tip="{loc:Localize Profile_RefreshDevices}">
                    <PathIcon Width="13" Height="13"
                              Data="M17.65 6.35A7.958 7.958 0 0 0 12 4a8 8 0 1 0 7.73 10h-2.08A6 6 0 1 1 12 6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"
                              Foreground="{DynamicResource VelaTextTertiary}" />
                  </Button>
                </Grid>
                <!-- 「已保存的 SSH 配置」选择器:选一条即让宿主代为建隧道,
                     插件收到的主机/端口已是本地转发端点。 -->
                <ComboBox ItemsSource="{Binding SshSessions}" SelectedItem="{Binding SelectedSshSession}"
                          IsVisible="{Binding IsSshSession}" HorizontalAlignment="Stretch">
                  <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:PluginSessionChoice">
                      <TextBlock Text="{Binding Display}" />
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
                <CheckBox IsChecked="{Binding Toggle}" IsVisible="{Binding IsToggle}"
                          Padding="0" MinHeight="0" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Label}" Margin="6,0,0,0"
                             Foreground="{DynamicResource VelaTextSecondary}"
                             FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium" />
                </CheckBox>
                <TextBlock Text="{Binding Hint}" IsVisible="{Binding HasHint}"
                           Foreground="{DynamicResource VelaTextMuted}"
                           FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 密码(密码认证;匿名 FTP 不需要。插件协议下标签由协议给,如 Secret Access Key) -->
        <StackPanel IsVisible="{Binding ShowPasswordField}">
          <TextBlock Classes="field-label" Text="{Binding PasswordLabel}" />
          <Border Classes="input-affix">
            <Grid ColumnDefinitions="*,Auto">
              <TextBox Grid.Column="0"
                       behaviors:SecurePasswordBox.Enabled="True"
                       behaviors:EnglishInputLocale.Enabled="True"
                       behaviors:SecurePasswordBox.Password="{Binding Password}"
                       behaviors:SecurePasswordBox.Reveal="{Binding ShowPassword}"
                       Padding="10,0,0,0"
                       VerticalAlignment="Center" />
              <Button Grid.Column="1" Width="28" Height="28"
                      Margin="0,0,2,0" Padding="0"
                      Background="Transparent" CornerRadius="3" Cursor="Hand"
                      Command="{Binding TogglePasswordVisibilityCommand}"
                      ToolTip.Tip="{loc:Localize Profile_ShowHidePassword}"
                      VerticalAlignment="Center">
                <PathIcon Width="13" Height="13"
                          Data="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"
                          Foreground="{DynamicResource VelaTextTertiary}" />
              </Button>
            </Grid>
          </Border>
        </StackPanel>

        <!-- 私钥(密钥认证;FTP 不支持) -->
        <StackPanel IsVisible="{Binding IsKeyAuth}" Spacing="14"
                    IsEnabled="{Binding RequiresSshAuth}">
          <StackPanel>
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_PrivateKeyFile}" />
            <Grid ColumnDefinitions="*,8,Auto">
              <TextBox Grid.Column="0" Text="{Binding PrivateKeyPath}"
                       PlaceholderText="~/.ssh/id_ed25519" />
              <Button Grid.Column="2" Classes="dlg-outline"
                      Content="{loc:Localize BrowseKeyFile}" Click="BrowseKeyFile_Click" />
            </Grid>
            <!-- 路径填错原先要等到连接失败,再从一句笼统的认证错误里猜。 -->
            <TextBlock Classes="field-error" Text="{Binding PrivateKeyPathError}"
                       IsVisible="{Binding PrivateKeyPathError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
          </StackPanel>
          <StackPanel>
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_PassphraseOptional}" />
            <TextBox Text="{Binding PrivateKeyPassphrase}"
                     behaviors:EnglishInputLocale.Enabled="True"
                     PasswordChar="&#x2022;" RevealPassword="False" />
          </StackPanel>
        </StackPanel>

        <!-- 记住密码 -->
        <StackPanel Orientation="Horizontal" Spacing="8"
                    IsVisible="{Binding ShowPasswordField}">
          <CheckBox IsChecked="{Binding RememberPassword}"
                    Padding="0" MinHeight="0" VerticalAlignment="Center">
            <TextBlock Text="{loc:Localize Profile_RememberPassword}"
                       Margin="6,0,0,0"
                       Foreground="{DynamicResource VelaTextSecondary}"
                       FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium" />
          </CheckBox>
          <TextBlock Text="{loc:Localize Profile_AesNote}"
                     Foreground="{DynamicResource VelaTextMuted}"
                     FontFamily="{DynamicResource VelaUiMonoFont}"
                     FontSize="{DynamicResource VelaFontSize10}"
                     VerticalAlignment="Center" />
        </StackPanel>

        <!-- 会话分组 + 显示名称 -->
        <Grid ColumnDefinitions="*,12,*">
          <StackPanel Grid.Column="0">
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_SessionGroup}" />
            <!-- 可编辑下拉:选已有分组,或直接输入新分组名(保存时不存在则自动建组)。 -->
            <ComboBox IsEditable="True"
                      ItemsSource="{Binding Groups}"
                      SelectedItem="{Binding SelectedGroup}"
                      Text="{Binding GroupText}"
                      PlaceholderText="{loc:Localize Profile_GroupPlaceholder}" />
          </StackPanel>
          <StackPanel Grid.Column="2">
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_DisplayName}" />
            <TextBox Text="{Binding Name}" PlaceholderText="web-prod-03" />
          </StackPanel>
        </Grid>

        <!-- 高级选项内容 -->
        <StackPanel IsVisible="{Binding IsAdvancedVisible}" Spacing="4">
          <TextBlock Classes="field-label" Text="{loc:Localize Profile_TagsLabel}" />
          <TextBox Text="{Binding TagsText}" PlaceholderText="{loc:Localize Profile_TagsPlaceholder}" />
          <TextBlock Classes="field-label" Text="{loc:Localize Profile_JumpHost}" Margin="0,8,0,0" />
          <ComboBox HorizontalAlignment="Stretch"
                    ItemsSource="{Binding JumpHostOptions}"
                    SelectedItem="{Binding SelectedJumpHost}" />
          <TextBlock Text="{loc:Localize Profile_JumpHostHint}"
                     Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />

          <!-- 本条配置专属的「认证后执行命令」。与设置 → 终端 → 会话那条全局命令是两件事:
               那条对所有终端生效,这条只跟着这一条配置走。只对 SSH 出现 —— 别的协议没有
               shell 通道,摆一个永远不会执行的输入框只会骗人。
               命令与延迟同排:延迟只有配了命令才有意义,拆成两栏会让人以为它是独立开关。 -->
          <StackPanel IsVisible="{Binding SupportsPostAuthCommand}">
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_PostAuthCommand}" Margin="0,8,0,5" />
            <Grid ColumnDefinitions="*,8,Auto">
              <TextBox Grid.Column="0" Text="{Binding PostAuthCommand}"
                       PlaceholderText="{loc:Localize Profile_PostAuthCommandPlaceholder}" />
              <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="{loc:Localize Profile_PostAuthDelay}"
                           Foreground="{DynamicResource VelaTextSecondary}"
                           FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium"
                           VerticalAlignment="Center" />
                <NumericUpDown Width="88" Value="{Binding PostAuthCommandDelaySeconds}"
                               Minimum="0" Maximum="{Binding MaxPostAuthCommandDelaySeconds}"
                               Increment="1" FormatString="0" ShowButtonSpinner="False" />
              </StackPanel>
            </Grid>
            <TextBlock Text="{loc:Localize Profile_PostAuthCommandHint}" Margin="0,4,0,0"
                       Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
          </StackPanel>

          <!-- SSH agent 转发。三态:勾 = 开、叉 = 关、不确定 = 跟随全局默认(设置 → 密钥管理)。
               只对 SSH 出现 —— 转发要在远端建 unix 套接字,别的协议连 shell 都没有。
               提示文字把代价写全:它是安全敏感开关,只写好处就是在骗人。 -->
          <StackPanel IsVisible="{Binding SupportsPostAuthCommand}" Margin="0,10,0,0">
            <CheckBox IsChecked="{Binding AgentForwarding}" IsThreeState="True"
                      Padding="0" MinHeight="0" VerticalAlignment="Center">
              <TextBlock Text="{loc:Localize Profile_AgentForwarding}"
                         Margin="6,0,0,0"
                         Foreground="{DynamicResource VelaTextSecondary}"
                         FontSize="{DynamicResource VelaFontSize11}" FontWeight="Medium" />
            </CheckBox>
            <TextBlock Text="{loc:Localize Profile_AgentForwardingHint}" Margin="0,4,0,0"
                       Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
          </StackPanel>

          <!-- 会话级终端覆盖(F-06):全局设置只能有一套,但机器不是一样的 ——
               堡垒机输出 GBK、开发机 UTF-8;生产环境希望标签一眼看出是红的。
               这些差异按机器走,挤进一个全局开关里就只能二选一。
               只对 SSH 出现:其余协议没有终端,摆一排永远不生效的下拉只会骗人。 -->
          <StackPanel IsVisible="{Binding SupportsPostAuthCommand}" Spacing="4">
            <TextBlock Classes="field-label" Text="{loc:Localize Profile_SessionOverrides}" Margin="0,12,0,0" />
            <TextBlock Text="{loc:Localize Profile_SessionOverridesHint}"
                       Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
            <Grid ColumnDefinitions="*,8,*" RowDefinitions="Auto,Auto,Auto,Auto" Margin="0,4,0,0">
              <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideEncoding}" />
                <ComboBox HorizontalAlignment="Stretch"
                          ItemsSource="{Binding EncodingOptions}"
                          SelectedItem="{Binding OverrideEncoding}" />
              </StackPanel>
              <StackPanel Grid.Row="0" Grid.Column="2">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideTerminalType}" />
                <ComboBox HorizontalAlignment="Stretch"
                          ItemsSource="{Binding TerminalTypeOptions}"
                          SelectedItem="{Binding OverrideTerminalType}" />
              </StackPanel>
              <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideColorScheme}" />
                <ComboBox HorizontalAlignment="Stretch"
                          ItemsSource="{Binding ColorSchemeOptions}"
                          SelectedItem="{Binding OverrideColorScheme}" />
              </StackPanel>
              <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,8,0,0">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideTabColor}" />
                <TextBox Text="{Binding OverrideTabColor}"
                         PlaceholderText="{loc:Localize Profile_OverrideTabColorPlaceholder}" />
              </StackPanel>
              <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,8,0,0">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideStartupDirectory}" />
                <TextBox Text="{Binding OverrideStartupDirectory}"
                         PlaceholderText="{loc:Localize Profile_OverrideStartupDirectoryPlaceholder}" />
              </StackPanel>
              <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,8,0,0">
                <TextBlock Classes="field-label" Text="{loc:Localize Profile_OverrideKeepAlive}" />
                <NumericUpDown Value="{Binding OverrideKeepAliveSeconds}"
                               Minimum="{Binding FollowGlobalKeepAlive}"
                               Maximum="{Binding MaxKeepAliveSeconds}"
                               Increment="10" FormatString="0" ShowButtonSpinner="False" />
              </StackPanel>
              <TextBlock Grid.Row="3" Grid.Column="2" Text="{loc:Localize Profile_OverrideKeepAliveHint}"
                         Margin="0,4,0,0"
                         Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
            </Grid>
          </StackPanel>

          <!-- FTP / FTPS 的「默认打开路径」:上传目标常年是同一个目录,而 FTP 给的登录工作目录
               往往就是根,每连一次手点四五层是纯粹的重复劳动。只对 FTP 出现——SSH/SFTP 走的是
               登录家目录,插件协议(S3…)自己在描述符里声明字段。 -->
          <StackPanel IsVisible="{Binding IsFtpSelected}">
            <TextBlock Classes="field-label" Text="{loc:Localize Ftp_InitialPath}" Margin="0,8,0,5" />
            <TextBox Text="{Binding FtpInitialRemotePath}"
                     PlaceholderText="{loc:Localize Ftp_InitialPathPlaceholder}" />
            <TextBlock Text="{loc:Localize Ftp_InitialPathHint}" Margin="0,4,0,0"
                       Foreground="{DynamicResource VelaTextMuted}" FontSize="{DynamicResource VelaFontSize10}" TextWrapping="Wrap" />
          </StackPanel>
        </StackPanel>

      </StackPanel>
      </ScrollViewer>

      <!-- 测试/保存反馈:**钉在滚动区外面**,紧贴按钮那一行。
           放在表单里(它原来的位置)时,长表单一滚动就把结论顶出视野 ——
           用户按下页脚的「测试」,眼睛盯着页脚,而回答落在了看不见的地方,
           于是界面看起来像是什么都没发生。Redis 有十来个字段,一定会滚。 -->
      <Border Grid.Row="3" Padding="20,8"
              IsVisible="{Binding HasFeedback}"
              Background="{DynamicResource VelaBgInput}"
              BorderThickness="0,1,0,0"
              BorderBrush="{DynamicResource VelaBorderPrimary}">
        <StackPanel Spacing="4">
          <!-- 失败原因常是一长串服务端原文(认证链、主机密钥指纹、栈顶异常):
               给它配一个复制按钮,用户才拿得走去搜索或贴给同事 —— 照着屏幕抄不现实。
               按钮与文案同排、钉在顶端:错误可能好几行,贴顶才不会随行数上下跑。 -->
          <Grid ColumnDefinitions="*,Auto"
                IsVisible="{Binding HasError}">
            <TextBlock Grid.Column="0" Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource VelaError}" FontSize="{DynamicResource VelaFontSize11}" TextWrapping="Wrap"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" x:Name="CopyErrorButton" Classes="copy-chip"
                    Margin="12,0,0,0"
                    Command="{Binding CopyErrorCommand}"
                    ToolTip.Tip="{loc:Localize Profile_CopyErrorTip}"
                    ToolTip.Placement="Top">
              <!-- 两态叠放而不是换文字:同宽同位,复制完按钮不会跳一下 -->
              <Panel>
                <StackPanel Orientation="Horizontal" Spacing="5" IsVisible="{Binding !ErrorCopied}">
                  <PathIcon Width="11" Height="11"
                            Data="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"
                            Foreground="{DynamicResource VelaTextSecondary}" />
                  <TextBlock Text="{loc:Localize Copy}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5" IsVisible="{Binding ErrorCopied}">
                  <PathIcon Width="11" Height="11"
                            Data="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"
                            Foreground="{DynamicResource VelaAccent}" />
                  <TextBlock Text="{loc:Localize Profile_ErrorCopied}" VerticalAlignment="Center"
                             Foreground="{DynamicResource VelaAccent}" />
                </StackPanel>
              </Panel>
            </Button>
          </Grid>
          <TextBlock Text="{loc:Localize Profile_TestSuccess}"
                     IsVisible="{Binding ShowTestSuccess}"
                     Foreground="{DynamicResource VelaAccent}" FontSize="{DynamicResource VelaFontSize11}" />
        </StackPanel>
      </Border>

      <!-- Footer -->
      <Border Grid.Row="4" Height="52" Padding="20,0"
              BorderThickness="0,1,0,0"
              BorderBrush="{DynamicResource VelaBorderPrimary}">
        <Grid ColumnDefinitions="Auto,*,Auto">
          <!-- 高级选项开关 -->
          <Button Grid.Column="0" Background="Transparent" Padding="0"
                  Cursor="Hand" Command="{Binding ToggleAdvancedCommand}"
                  VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <PathIcon Width="12" Height="12"
                        Data="M3 17v2h6v-2H3zM3 5v2h10V5H3zm10 16v-2h8v-2h-8v-2h-2v6h2zM7 9v2H3v2h4v2h2V9H7zm14 4v-2H11v2h10zm-6-4h2V7h4V5h-4V3h-2v6z"
                        Foreground="{DynamicResource VelaTextTertiary}" />
              <TextBlock Text="{loc:Localize Profile_AdvancedOptions}"
                         Foreground="{DynamicResource VelaTextTertiary}"
                         FontSize="{DynamicResource VelaFontSize11}" VerticalAlignment="Center" />
              <!-- 折叠时收走了几个插件字段就报几个:没有这个数字,S3 的调优项在用户眼里
                   就是"设置项不见了"。 -->
              <Border IsVisible="{Binding HasAdvancedBadge}"
                      Background="{DynamicResource VelaBgInput}"
                      BorderBrush="{DynamicResource VelaBorderPrimary}" BorderThickness="1"
                      CornerRadius="8" Padding="6,1" VerticalAlignment="Center">
                <TextBlock Text="{Binding AdvancedBadge}"
                           Foreground="{DynamicResource VelaTextSecondary}"
                           FontFamily="{DynamicResource VelaUiMonoFont}"
                           FontSize="{DynamicResource VelaFontSize10}" />
              </Border>
            </StackPanel>
          </Button>

          <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8"
                      VerticalAlignment="Center">
            <Button Classes="dlg-outline" Command="{Binding TestConnectionCommand}">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <PathIcon Width="12" Height="12"
                          Data="M11 21h-1l1-7H7.5c-.58 0-.57-.32-.38-.66.19-.34.05-.08.07-.12C8.48 10.94 10.42 7.54 13 3h1l-1 7h3.5c.49 0 .56.33.47.51l-.07.15C12.96 17.55 11 21 11 21z"
                          Foreground="{DynamicResource VelaTextSecondary}" />
                <TextBlock Text="{loc:Localize Profile_Test}" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Classes="dlg-outline" Command="{Binding SaveCommand}">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <PathIcon Width="12" Height="12"
                          Data="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z"
                          Foreground="{DynamicResource VelaTextSecondary}" />
                <TextBlock Text="{loc:Localize Save}" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Classes="dlg-primary" Command="{Binding ConnectCommand}">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <PathIcon Width="12" Height="12"
                          Data="M16 7V3h-2v4h-4V3H8v4c-1.1 0-2 .9-2 2v5.5L9.5 18v3h5v-3L18 14.5V9c0-1.1-.9-2-2-2z"
                          Foreground="{DynamicResource VelaAccentForeground}" />
                <TextBlock Text="{loc:Localize Connect}" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>
        </Grid>
      </Border>

    </Grid>
  </Border>
</Window>
