# 相似度门禁白名单
#
# 这里列的是**必然与他人实现相同、且相同不构成侵权**的文件 ——
# 协议规定的事实，表达方式唯一（merger doctrine / scènes à faire）。
# 见 NOTICE.md 的「关于独立性」与 src/VelaShell.Ssh/AGENTS.md §2 的「例外」。
#
# ⚠️ 加一行进来，就等于宣称「这个文件里没有表达性内容，只有协议事实」。
#    每一行都必须写明理由。**没有理由的行会在 review 时被打回。**
#    拿不准时不要加 —— 重写那一段的成本，远低于把一个真问题藏进白名单的成本。
#
# 路径相对仓库根，用正斜杠。

# 消息编号表：RFC 4250 §4.1.2 规定的数值与名字。任何正确的实现都必然相同。
src/VelaShell.Ssh/Protocol/SshMessageNumber.cs

# 断开原因码表：RFC 4250 §4.2.2 规定的名字与数值。同上，协议事实。
# 它刻意从 SshExceptions.cs 拆出来，好让那个文件里的异常层级仍然受监视。
src/VelaShell.Ssh/Diagnostics/SshDisconnectReason.cs

# SFTP 的四张码表:报文类型、状态码、pflags、ATTRS 位。
# draft-ietf-secsh-filexfer-02 §3 / §7 / §6.3 / §5 规定的名字与数值 —— 协议事实。
# 它刻意从 SftpConstants.cs 拆出来,好让那个文件里**我们自己选的**常量
# (块大小、上限、默认权限)仍然受监视。
src/VelaShell.Ssh/Sftp/SftpProtocolCodes.cs
