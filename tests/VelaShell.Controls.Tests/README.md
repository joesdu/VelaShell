# VelaShell.Controls.Tests

> [`VelaShell.Controls`](../../src/VelaShell.Controls) 自定义控件的单元测试。

验证复用控件在 Avalonia headless 环境下的行为。

## 覆盖范围

| 文件 | 被测对象 |
|------|----------|
| `StatusMetricChipTests` | `StatusMetricChip` 状态指标胶囊控件的属性与呈现逻辑。 |

## 运行

```bash
dotnet test tests/VelaShell.Controls.Tests/
```
